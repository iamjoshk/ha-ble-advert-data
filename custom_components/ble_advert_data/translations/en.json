{
  "config": {
    "step": {
      "user": {
        "title": "Select a Bluetooth device",
        "description": "Choose a Bluetooth device that Home Assistant can see to record its advertisement data. You can type to filter or paste a MAC address.",
        "data": {
          "address": "Device"
        }
      }
    },
    "error": {
      "no_devices": "No Bluetooth devices are currently visible. Make sure the device is advertising and try again."
    },
    "abort": {
      "already_configured": "This device is already configured."
    }
  },
  "entity": {
    "sensor": {
      "advertisement": {
        "name": "Advertisement data"
      },
      "latest_mac": {
        "name": "Latest MAC address"
      },
      "gatt_battery": {
        "name": "Battery"
      }
    },
    "binary_sensor": {
      "connectivity": {
        "name": "Connected"
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "menu_options": {
          "gatt_settings": "GATT settings",
          "service_data_fingerprint": "Service data fingerprinting",
          "add_rule": "Add a parsing rule",
          "edit_rule": "Edit a parsing rule",
          "remove_rule": "Remove a parsing rule"
        }
      },
      "gatt_settings": {
        "title": "GATT settings",
        "description": "Enable GATT fetching to read battery levels from Bluetooth devices that expose the standard battery service (UUID 180F).",
        "data": {
          "enable_gatt_fetch": "Enable GATT fetching"
        }
      },
      "service_data_fingerprint": {
        "title": "Service data fingerprinting",
        "description": "For devices with randomized MAC addresses, match on service data instead. The fingerprint is matched as a prefix of the service data. Leave empty to disable.",
        "data": {
          "enable_fingerprint": "Enable fingerprint matching",
          "service_uuid": "Service UUID",
          "fingerprint": "Hex fingerprint (e.g., 0050d281)"
        }
      },
      "add_rule": {
        "title": "Add a parsing rule",
        "description": "Define how to read bytes from the advertisement. The source key is required for manufacturer data (decimal or 0x hex) and service data (UUID).",
        "data": {
          "name": "Sensor name",
          "source_type": "Source type",
          "source_key": "Source key",
          "offset": "Byte offset",
          "length": "Byte length",
          "endian": "Byte order",
          "signed": "Signed",
          "scale": "Scale",
          "unit": "Unit of measurement"
        }
      },
      "edit_rule": {
        "title": "Edit a parsing rule",
        "description": "Define how to read bytes from the advertisement. The source key is required for manufacturer data (decimal or 0x hex) and service data (UUID).",
        "data": {
          "id": "Rule",
          "name": "Sensor name",
          "source_type": "Source type",
          "source_key": "Source key",
          "offset": "Byte offset",
          "length": "Byte length",
          "endian": "Byte order",
          "signed": "Signed",
          "scale": "Scale",
          "unit": "Unit of measurement"
        }
      },
      "remove_rule": {
        "title": "Remove a parsing rule",
        "description": "Choose a parsing rule to remove.",
        "data": {
          "id": "Rule"
        }
      }
    },
    "error": {
      "unknown_error": "An unexpected error occurred"
    }
  }
}
